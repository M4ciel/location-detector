import { AppService } from './app.service';

describe('AppService', () => {
  let appService: AppService;

  describe('findByJson', () => {
    it('should return the path of the created file', async () => {
      return;
    });
  });

  describe('findByCsv', () => {
    it('should return the path of the created file', async () => {
      return;
    });
  });

  describe('alocateStoreage', () => {
    it('should return a valid file path', async () => {
      return;
    });
  });
});
